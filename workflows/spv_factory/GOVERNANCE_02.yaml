task_id: GOVERNANCE_02
name: Confirm management and signing authority
phase: governance
inputs:
  - proposed_management_structure
  - proposed_signing_authority
dependencies:
  - FORMATION_05
decision:
  required: true
  authority: authorized_company_representative
  record: management_and_signing_authority_record
execution:
  mode: human
  type: manual
  worker: project_owner
approval:
  required: false
evidence:
  - management_and_signing_authority_record
completion:
  - manager_confirmed
  - signing_authority_confirmed
  - evidence_saved
escalate_if:
  - authority_unclear
  - conflicting_instructions
